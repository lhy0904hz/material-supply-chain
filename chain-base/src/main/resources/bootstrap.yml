server:
  port: 8000
spring:
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 200MB
  application:
    name: chain-base
  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: 127.0.0.1:8848
        namespace: public
        group: public
      config:
        ###配置中心连接地址
        server-addr: 127.0.0.1:8848
  redis:
    database: 0
    host: localhost
    port: 6379
    password:

